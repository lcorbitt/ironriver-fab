export const REVEAL_DURATION_MS = 1000;
export const REVEAL_TRANSLATE_PX = 20;

/** Minimum fraction of the element visible before revealing (0–1). */
export const REVEAL_INTERSECTION_THRESHOLD = 0.5;
